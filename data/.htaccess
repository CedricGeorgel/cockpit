# Interdit tout accès direct aux JSON : ils ne passent QUE par relay.php (jeton).
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
